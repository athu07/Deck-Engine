# J2W Pre-sales Deck Engine - Python dependencies
# Pinned to the versions verified working locally (2026-07-02).
# Install with:  py -m pip install -r requirements.txt

# Web app
Flask==3.1.3

# PowerPoint generation / editing
python-pptx==1.0.2
lxml==6.1.1

# Spreadsheet reading (registry, skills inventory, case-study source)
openpyxl==3.1.5

# AI matching / drafting (OpenAI gpt-4o-mini + text-embedding-3-small)
openai==2.42.0

# PDF / research-file reading (deep-research briefs + stakeholder profiles).
# BOTH are required: research.py reads with pypdf first, then falls back to
# PyMuPDF (imported as "fitz"). Missing these is why PDF reading failed on deploy.
pypdf==6.11.0
PyMuPDF==1.27.2.3

# Production WSGI server (deployment only)
gunicorn
